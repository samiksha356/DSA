#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,x, res=0;
    scanf("%d", &n);
    for(int i=0; i<n; i++){
        scanf("%d", &x);
        res^=x;
    }
    printf("%d", res);
    return 0;
}
